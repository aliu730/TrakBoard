const App = require('./components/app.jsx');
const ReactDOM = require('react-dom');
const React = require('react');
import 'normalize.css';

ReactDOM.render(<App />, document.getElementById('mainDisplay'));
